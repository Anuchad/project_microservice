pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          sh '''
              ls -l
              git status
              git branch
              git checkout -b test
              git branch
          '''
        //}
      }
    }
  }
}
