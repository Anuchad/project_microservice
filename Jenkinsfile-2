pipeline {
  //agent { dockerfile true }
  agent any
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          def branch = env.BRANCH_NAME
          echo "Building branch: ${branch}"

         // sh '''
             // ls -l
             // git status
             // git branch
          //'''
        //}
      }
    }
  }
}
