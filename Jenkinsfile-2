pipeline {
  //agent { dockerfile true }
  agent any
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          script {

            def eventType = env.EVENT_TYPE
            def repositoryName = env.REPOSITORY_NAME
            def branchName = env.BRANCH_NAME
            
            echo "Event type: ${eventType}"
            echo "Repository name: ${repositoryName}"
            echo "Branch name: ${branchName}"
            
            echo "Event type: ${eventType}"
            echo "Repository name: ${repositoryName}"
  
            sh '''
                ls -l
                git status
                git branch
            '''
          }
        //}
      }
    }
  }
}
