pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          cd /var/www/html
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}
