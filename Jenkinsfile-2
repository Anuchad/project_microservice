pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          sh '''
              ls -l
              node -v
              git --version
          '''
        //}
      }
    }
  }
}
