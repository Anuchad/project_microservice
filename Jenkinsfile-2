pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          echo "Building branch: ${branch}"

          sh '''
              ls -l
              git status
              git branch
          '''
        //}
      }
    }
  }
}
