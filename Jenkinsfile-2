pipeline {
  //agent { dockerfile true }
  agent any
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          script {
            
            echo "Event : ${env}"
  
            sh '''
                ls -l
                git status
                git branch
            '''
          }
        //}
      }
    }
  }
}
