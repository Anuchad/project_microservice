pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        dir('/var/www/html') {
          sh '''
              node --version
              git --version
              curl --version
          '''
        }
      }
    }
  }
}
