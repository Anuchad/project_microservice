pipeline {
  //agent { dockerfile true }
  agent any
  environment {
      BRANCH = "${ref.split('/')[2]}"
  }
  stages {
    stage('Build') {
      steps {
        script {
          currentBuild.displayName = "Build Repo : ${repo} ${env.BUILD_ID}"
          currentBuild.description = "Commit By: ${commit-author} \n Commit Message: ${commit-message} \n Commit Time: ${commit-time}"
        }
      }
    }
    stage('branch main') {
      when {
        anyOf {
          expression {BRANCH == 'main'}
        }
      }
      steps {
          script {
            echo "Received webhook payload: ${repo}"
            echo "Received webhook ref: ${BRANCH}"
          }
      }
    }
    stage('branch hook') {
      when {
        anyOf {
          expression {BRANCH == 'test-hook'}
        }
      }
      steps {
          script {
            echo "Received webhook payload: ${repo}"
            echo "Received webhook ref: ${BRANCH}"
          }
      }
    }
  }
}
