pipeline {
  //agent { dockerfile true }
  agent any
  stages {
    stage('Test') {
      steps {
        //dir('var') {
          script {
            
            echo "Event : ${payload}"
  
            //sh '''
                //ls -l
                //git status
                //git branch
            //'''
            sh "printenv | sort"
          }
        //}
      }
    }
  }
}
