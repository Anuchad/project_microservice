pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat "echo 'build'"
            }
        }

        stage('Scan') {
            steps {
                bat "echo 'scane'"
            }
        }
    }
}
